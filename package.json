{
  "name": "lexia-bancaria",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @lexia/web",
    "build": "npm run build --workspace @lexia/web",
    "lint": "npm run lint --workspace @lexia/web",
    "typecheck": "npm run typecheck --workspace @lexia/web",
    "test": "npm run test --workspace @lexia/web"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "js-yaml": "^4.1.1"
  }
}
